---
title: 反无人机光电转台深度解析：可见光与热成像相机原理及仿真建模实战
date: 2026-04-23 16:00:00
categories:
  - 无人机
  - 仿真开发
  - 传感器
tags:
  - 光电转台
  - PTZ
  - 热成像
  - 红外相机
  - 可见光相机
  - LWIR
  - VOx
  - 反无人机
  - Gazebo
  - AirSim
  - Unreal Engine
  - 传感器仿真
  - NETD
  - CMOS
  - 伺服控制
---

> 一台反无人机光电转台（EO/IR PTZ）的核心不是那个能 360° 旋转的机械结构，而是里面搭载的**两只眼睛**——可见光相机和热成像仪。前者在白天提供高分辨率彩色图像用于目标识别取证，后者在任何光照条件下捕捉目标的热辐射特征实现全天候探测。两者协同，构成了对低空小目标从"发现"到"确认"的完整感知链。
>
> 本文以一款实际量产的反无人机光电转台（和普威视 HP-Z50III）的技术规格书为蓝本，深入拆解其搭载的**星光级可见光相机**与**非制冷 LWIR 热成像仪**的工作原理、核心参数与工程设计，然后系统讲解如何在 Gazebo Sim、AirSim/Colosseum、Unreal Engine 等主流仿真平台中对这两类传感器进行建模，并给出可直接运行的配置示例。
>
> 前置阅读：
> - [无人机视觉仿真的 8 道鸿沟：从理想相机到真实传感器的差距与弥合](/2026/04/21/无人机视觉仿真的8道鸿沟-从理想相机到真实传感器的差距与弥合/)
> - [AirSim 相机传感器深度解析：架构、实现与优化实战](/2026/03/10/AirSim相机传感器深度解析-架构实现与优化实战/)

---

## 一、光电转台在反无人机系统中的角色

### 1.1 系统架构

一套完整的反无人机系统通常由**探测层 → 跟踪确认层 → 处置层**三级构成：

{% mermaid %}
graph LR
    A[雷达/RF频谱<br>粗探测] -->|方位+距离| B[光电转台 PTZ<br>精跟踪+确认]
    B -->|目标类型+坐标| C[干扰/打击<br>处置]
    B -->|视频流+AI检测| D[操作员<br>人工确认]
{% endmermaid %}

光电转台处于核心的**跟踪确认层**——雷达或 RF 频谱探测器给出粗略方位后，PTZ 迅速转向该方位，利用可见光和热成像两个通道锁定并持续跟踪目标，同时由嵌入式 AI 或后端服务器进行目标分类（无人机 vs 鸟类 vs 其他），最终为处置决策提供依据。

### 1.2 HP-Z50III 光电转台概览

以和普威视 HP-Z50III 型光电转台为例（实际型号 HP-Z50IIIBL-TC61023-VC4075-AU-SFW7），这是一款面向军事基地、机场、监狱等场所设计的光电探测跟踪一体化系统：

| 维度 | 规格 |
|------|------|
| 外形 | 球型，Φ565mm × H880mm |
| 重量 | ≤110 kg |
| 防护 | IP67，-40°C ～ 60°C |
| 水平回转 | N × 360° 连续旋转 |
| 俯仰范围 | -90° ～ +90° |
| 回转速度 | 0.01° ～ 120°/s（水平），0.01° ～ 90°/s（俯仰） |
| 加速度 | 120°/s²（水平/俯仰） |
| 定位精度 | 优于 **0.005°** |
| 跟踪精度 | ≤ 0.15 mrad |
| 驱动方式 | 稀土永磁同步电机直驱 |
| 传感器 | **可见光 + 热成像**（双光合一） |
| AI 能力 | 嵌入式人/车/船/无人机识别、自动跟踪 |

**双视窗设计**使可见光和热成像镜头同轴安装，最长焦时两通道中心视场偏差 ≤ 1/20 视场角，保证目标在两个通道中始终处于同一位置。

---

## 二、可见光相机：星光级 CMOS 的工作原理

### 2.1 核心参数解读

HP-Z50III 搭载的可见光通道参数如下：

| 参数 | 规格 | 工程含义 |
|------|------|----------|
| 传感器 | 1/1.8″ 星光级 CMOS | 单像素面积大，低光性能优异 |
| 分辨率 | **2688 × 1520**（约 400 万像素） | 4MP，主流安防级别 |
| ICR | 彩转黑自动切换 | 白天彩色，夜间自动切换黑白+近红外增强 |
| 最低照度 | 彩色 0.002 Lux / 黑白 0.0002 Lux | 月光级甚至星光级可用 |
| 焦距范围 | **12.5 ～ 775 mm**（62 倍光学变焦） | 广角 33.4° 到窄角 0.58° |
| 光圈 | F3.5 ～ F8.3 | 长焦端收光圈，平衡景深与进光量 |
| 帧率 | 50 Hz | PAL 制式 |
| 编码 | H.264 / H.265 / MJPEG | 网络传输 |
| 增强功能 | 宽动态、强光抑制、电子防抖、3D 降噪、光电/电子双重透雾 | 复杂环境适应 |

### 2.2 星光级 CMOS 的关键技术

**"星光级"** 是安防行业术语，指在极低照度（＜0.01 Lux）下仍能输出可用图像的能力。其核心技术手段：

**（1）大像素面积**

1/1.8″ 靶面（对角线约 8.93 mm）配合 400 万像素，单像素尺寸约 **2.8 µm**。像素面积越大，单位时间内捕获的光子数越多，信噪比（SNR）越高。

$$
\text{SNR} \propto \frac{\text{信号光子数}}{\sqrt{\text{信号光子数} + \text{暗电流噪声} + \text{读出噪声}^2}}
$$

在极低照度下，信号光子数极少，暗电流和读出噪声成为主要矛盾。星光级 CMOS 通过以下手段降低噪声：

- **背照式（BSI）架构**：将电路层移到感光层下方，量子效率从 40% 提升到 **80%+**
- **深沟槽隔离（DTI）**：像素间物理隔离，防止电荷串扰
- **片上降噪**：CDS（相关双采样）+ 列级 ADC，读出噪声降至 **＜2 e⁻**

**（2）ICR 彩转黑**

ICR（IR-Cut filter Removable）是一个可机械切换的红外截止滤光片：

| 模式 | 滤光片状态 | 感光波段 | 输出 |
|------|-----------|---------|------|
| 白天 | 插入（IR-Cut ON） | 400-700 nm（可见光） | 彩色图像 |
| 夜间 | 移除（IR-Cut OFF） | 400-1000 nm（可见光+近红外） | 黑白图像 |

夜间模式下，CMOS 额外接收 700-1000 nm 的近红外光（月光、星光中包含大量近红外成分），进光量可增加 **3-5 倍**，这是实现 0.0002 Lux 最低照度的关键。

**（3）62 倍光学变焦**

12.5 mm → 775 mm 的焦距范围意味着：

| 焦距 | 水平视场角 | 3 km 处单像素覆盖 | 用途 |
|------|-----------|------------------|------|
| 12.5 mm | 33.4° | ~6.5 m | 广域搜索 |
| 100 mm | ~4.2° | ~0.8 m | 中距跟踪 |
| 775 mm | 0.58° | **~0.1 m** | 远距识别取证 |

在 775 mm 焦距下，3 km 处一架 0.3 m 跨度的微型无人机可以占据约 **3 个像素**——这是识别的下限。

### 2.3 可见光通道的探测性能

根据规格书，对 0.3m × 0.3m 无人机目标（能见度 ≥20 km，湿度 ≤50% RH）：

| 能力 | 距离 |
|------|------|
| 探测（发现有目标） | ≥ 5000 m |
| 跟踪（持续锁定） | ≥ 4000 m |
| 识别（判断类型） | ≥ 3000 m |

这些距离遵循 **Johnson 准则**（DRI 准则）：

| 任务 | 目标上需要的像素数（跨度方向） |
|------|------------------------------|
| 探测 Detection | ≥ 1.5 线对 ≈ **3 像素** |
| 识别 Recognition | ≥ 4 线对 ≈ **8 像素** |
| 辨认 Identification | ≥ 6.5 线对 ≈ **13 像素** |

### 2.4 可见光通道的局限

| 局限 | 影响 |
|------|------|
| **光照依赖** | 彩色模式需 ≥0.002 Lux，极端黑暗（无月无星）下即使切黑白也困难 |
| **大气散射** | 雾霾天能见度下降，虽有电子透雾但远距性能严重衰减 |
| **强光干扰** | 朝阳/夕阳方向目标被淹没，宽动态虽有帮助但不根治 |
| **无温度信息** | 纯光学成像，无法区分温度特征相似的目标 |

这些局限正是热成像仪存在的理由。

---

## 三、热成像仪：非制冷 VOx 焦平面的工作原理

### 3.1 核心参数解读

HP-Z50III 搭载的红外热成像通道参数如下：

| 参数 | 规格 | 工程含义 |
|------|------|----------|
| 探测器 | **非制冷 VOx（氧化钒）红外焦平面** | 无需制冷机，免维护 |
| 光谱范围 | **8 ～ 14 µm（LWIR）** | 长波红外，大气窗口 |
| 分辨率 | **640 × 512** | 非制冷主流规格 |
| NETD | **优于 30 mK** | 可分辨 0.03°C 温差 |
| 镜头焦距 | **23 ～ 230 mm**（10 倍连续变焦），F1.5 | 大光圈保证进光量 |
| 视场角 | 1.91° ～ 18.47° | 窄角到中等视场 |
| 帧率 | 50 Hz | 与可见光同步 |
| 图像增强 | SDE 数字细节增强 | 边缘锐化、对比度自适应 |
| 极性/伪彩 | 热黑、热白、多种伪彩色 | 操作员可按需切换 |
| 编码 | H.264 / H.265 / MJPEG | 网络传输 |

### 3.2 非制冷微辐射热计的成像原理

热成像仪与可见光相机有着**完全不同的成像原理**：

{% mermaid %}
graph TB
    subgraph 可见光相机
    A1[物体反射/发射可见光<br>0.4-0.7µm] --> B1[光学镜头聚焦]
    B1 --> C1[CMOS 光电转换<br>光子→电子]
    C1 --> D1[数字图像<br>亮度=反射光强度]
    end
    subgraph 热成像仪
    A2[物体自身热辐射<br>8-14µm] --> B2[红外光学镜头<br>锗/硫化锌]
    B2 --> C2[微辐射热计<br>热→电阻变化]
    C2 --> D2[温度图<br>亮度=物体温度]
    end
{% endmermaid %}

**关键区别**：可见光相机测量的是目标**反射**的光，而热成像仪测量的是目标自身**发射**的热辐射。

**（1）普朗克黑体辐射定律**

任何温度大于绝对零度的物体都会向外辐射电磁波，其辐射光谱由普朗克公式决定：

$$
M(\lambda, T) = \frac{2\pi hc^2}{\lambda^5} \cdot \frac{1}{e^{hc/\lambda k_B T} - 1}
$$

其中 $h$ 为普朗克常数，$c$ 为光速，$k_B$ 为玻尔兹曼常数，$\lambda$ 为波长，$T$ 为绝对温度。

对于常温物体（-20°C ～ 60°C，即 253K ～ 333K），根据维恩位移定律，峰值辐射波长约为 **8.7 ～ 11.4 µm**——正好落在 **LWIR 8-14 µm** 大气窗口内。

**（2）VOx 微辐射热计**

非制冷型红外探测器的核心是**微辐射热计（Microbolometer）**像素阵列：

| 组件 | 作用 |
|------|------|
| 红外吸收层 | 吸收 8-14 µm 红外辐射，将光能转化为热能 |
| VOx 热敏电阻 | 温度变化导致电阻变化（TCR ≈ -2%/K） |
| 微桥结构 | 将热敏电阻悬空，减小热容，提高响应速度 |
| 读出电路（ROIC） | 测量每个像素的电阻变化，转换为电信号 |

每个像素的工作流程：

$$
\text{入射红外辐射} \xrightarrow{\text{吸收}} \Delta T_{\text{像素}} \xrightarrow{\text{VOx TCR}} \Delta R \xrightarrow{\text{ROIC}} \Delta V \xrightarrow{\text{ADC}} \text{数字值}
$$

**（3）NETD——热灵敏度的核心指标**

NETD（Noise Equivalent Temperature Difference，噪声等效温差）是衡量热成像仪灵敏度的最重要指标：

$$
\text{NETD} = \frac{V_{\text{noise}}}{\partial V / \partial T}
$$

HP-Z50III 的 NETD 优于 **30 mK**，意味着它能分辨 **0.03°C** 的温差。对比：

| 目标 | 与背景典型温差 | 30 mK NETD 下的 SNR |
|------|--------------|---------------------|
| 人体（37°C）vs 夏天地面（30°C） | ~7°C | ~233 |
| 无人机电机（60°C）vs 天空（-20°C） | ~80°C | ~2667 |
| 无人机电池（45°C）vs 天空（-20°C） | ~65°C | ~2167 |
| 鸟类（40°C）vs 天空（-20°C） | ~60°C | ~2000 |

**无人机的热特征在天空背景中极其显著**——即使是微型四旋翼（0.3m 级别），其电机温度也能达到 60-80°C，与天空背景有 80-100°C 的温差，在热像上呈现高亮点。

**（4）红外光学材料**

热成像仪不能使用普通玻璃镜头（玻璃在 LWIR 波段不透明），需要使用特殊红外光学材料：

| 材料 | 透射波段 | 折射率 | 用途 |
|------|---------|--------|------|
| **锗（Ge）** | 2-14 µm | 4.0 | LWIR 主流镜头材料 |
| 硫化锌（ZnS） | 0.4-14 µm | 2.2 | 双波段/多光谱 |
| 硒化锌（ZnSe） | 0.5-22 µm | 2.4 | 宽波段窗口 |
| 硫系玻璃 | 1-14 µm | 2.5 | 模压非球面镜 |

HP-Z50III 的红外镜头 **F1.5** 大光圈设计是非制冷探测器的必需——非制冷探测器灵敏度低于制冷型，需要大光圈收集更多红外辐射来补偿。

### 3.3 热成像通道的探测性能

对 0.3m × 0.3m 无人机目标（温差 > 6K）：

| 能力 | 距离 |
|------|------|
| 探测 | ≥ 2200 m |
| 跟踪 | ≥ 1700 m |
| 识别 | ≥ 1200 m |

热成像的探测距离短于可见光，原因是分辨率差距巨大（640×512 vs 2688×1520，像素数差 **~6.4 倍**）。但热成像的优势在于**全天候能力**——黑夜、雾霾、无光照条件下，可见光完全失效，而热成像依然可用。

### 3.4 两种传感器的互补关系

| 维度 | 可见光 | 热成像 | 协同效果 |
|------|--------|--------|----------|
| 工作时段 | 白天/有光照 | **全天候** | 24 小时覆盖 |
| 探测距离 | 远（5 km+） | 中（2.2 km） | 可见光先发现，热像接力确认 |
| 分辨率 | 高（4MP） | 低（640×512） | 可见光取证，热像探测 |
| 目标特征 | 外观、颜色、形状 | **温度** | 双重特征融合识别 |
| 雾霾性能 | 差 | **好（LWIR 穿透力强）** | 恶劣天气热像主导 |
| 伪装对抗 | 光学伪装有效 | **热伪装极难** | 反伪装能力强 |
| 成本 | 低 | 高 | 双光比三光（含 LRF）经济 |

---

## 四、伺服转台：精密指向与跟踪控制

PTZ 的机械伺服系统决定了传感器能否快速、精确地指向目标。

### 4.1 关键伺服参数

| 参数 | HP-Z50III 规格 | 工程意义 |
|------|---------------|----------|
| 回转速度 | 120°/s | 对 2 km 处 50 m/s 横飞目标，角速度约 1.4°/s，裕量充足 |
| 加速度 | 120°/s² | 从静止到最大速度仅需 1 秒 |
| 定位精度 | 0.005° | 在 3 km 处对应 **0.26 m** 指向误差 |
| 跟踪精度 | 0.15 mrad | 在 3 km 处对应 **0.45 m** 跟踪误差 |
| 驱动方式 | 直驱电机 | 无齿轮传动间隙，回差为零 |
| 预置位 | ≥ 2048 个 | 可编程巡航路径 |
| 焦距速度自适应 | 支持 | 长焦窄视场时自动降速，防止目标丢失 |

### 4.2 焦距-速度自适应

这是一个重要的工程细节：当镜头变焦到长焦（窄视场）时，相同的角速度会导致图像中目标移动更快，容易脱出视场。HP-Z50III 的伺服系统会自动根据当前焦距调节回转速度上限：

$$
v_{\max}(\text{调整后}) = v_{\max} \times \frac{\text{FOV}_{\text{当前}}}{\text{FOV}_{\text{广角}}}
$$

例如：热像 230 mm 焦距（FOV 1.91°）时，回转速度上限自动降至约 120° × (1.91/18.47) ≈ **12.4°/s**，确保目标不会因转动过快而飞出视场。

---

## 五、AI 目标检测与跟踪链路

HP-Z50III 内置嵌入式 AI 检测跟踪能力，其与外部智能分析服务器的协作链路如下：

{% mermaid %}
graph TB
    A[可见光/热像 视频流] --> B[嵌入式 AI<br>前端检测]
    B -->|RTSP 视频流| C[智能分析服务器<br>39020 端口]
    A -->|img: 报文头+JPEG| C
    C -->|JSON 检测结果<br>bbox+class+score| D[光电主控]
    D -->|云台控制指令| E[伺服驱动]
    B -->|本地跟踪| E
    D -->|报警/视频| F[指控中心]
{% endmermaid %}

目标检测结果的数据格式（来自智能分析服务通信协议）：

```json
{
    "Info": "{\"chn\":0,\"width\":704,\"height\":576,\"timestamp\":23344890933}",
    "Result": [
        {
            "X1": "397", "Y1": "414",
            "X2": "443", "Y2": "463",
            "ID": 1,
            "Class": "car-sedan",
            "Score": "0.947316"
        }
    ]
}
```

检测框 `(X1,Y1)-(X2,Y2)` 为像素坐标，`Class` 支持人/车/船/无人机等类别，`Score` 为置信度。系统支持跟踪状态切换和跟踪位置同步，实现从检测到跟踪的无缝衔接。

---

## 六、仿真建模：可见光相机

在仿真中精确模拟可见光相机，需要从**内参 → 外参 → 图像效果**三个层次进行建模。

### 6.1 核心参数映射

首先需要将物理相机参数转换为仿真参数：

| 物理参数 | 值 | 仿真参数 | 计算/映射 |
|----------|---|----------|-----------|
| 传感器尺寸 | 1/1.8″ (7.18×5.32 mm) | — | 用于计算 FOV |
| 分辨率 | 2688×1520 | `<width>` `<height>` | 直接映射 |
| 焦距范围 | 12.5-775 mm | `<horizontal_fov>` | 计算 FOV |
| 帧率 | 50 Hz | `<update_rate>` | 直接映射 |
| 最小照度 | 0.002 Lux | 噪声模型参数 | 需自定义 |

**FOV 计算**：

$$
\text{HFOV} = 2 \cdot \arctan\left(\frac{w_{\text{sensor}}}{2f}\right)
$$

| 焦距 | HFOV（计算值） | 规格书标称 |
|------|---------------|-----------|
| 12.5 mm | 32.1° | 33.4° |
| 775 mm | 0.53° | 0.58° |

计算值与标称值接近，差异来自镜头畸变补偿。

### 6.2 Gazebo Sim 配置

```xml
<sensor type="camera" name="eo_camera">
  <update_rate>50</update_rate>
  <camera name="visible_light">
    <horizontal_fov>0.583</horizontal_fov>  <!-- 33.4° 广角端 -->
    <image>
      <width>2688</width>
      <height>1520</height>
      <format>R8G8B8</format>
    </image>
    <clip>
      <near>0.5</near>
      <far>10000</far>
    </clip>
    <noise>
      <type>gaussian</type>
      <mean>0.0</mean>
      <stddev>0.005</stddev>  <!-- 低光噪声模拟 -->
    </noise>
    <distortion>
      <k1>-0.25</k1>
      <k2>0.12</k2>
      <k3>0.0</k3>
      <p1>0.0</p1>
      <p2>0.0</p2>
      <center>0.5 0.5</center>
    </distortion>
    <lens>
      <intrinsics>
        <fx>4596.0</fx>   <!-- f_mm / pixel_pitch_mm -->
        <fy>4596.0</fy>
        <cx>1344.0</cx>
        <cy>760.0</cy>
        <s>0</s>
      </intrinsics>
    </lens>
  </camera>
  <always_on>1</always_on>
  <visualize>true</visualize>
  <topic>eo_camera</topic>
</sensor>
```

### 6.3 AirSim / Colosseum 配置

在 `settings.json` 中配置：

```json
{
  "CameraDefaults": {
    "CaptureSettings": [
      {
        "ImageType": 0,
        "Width": 2688,
        "Height": 1520,
        "FOV_Degrees": 33.4,
        "TargetGamma": 1.0,
        "MotionBlurAmount": 0.0
      }
    ]
  }
}
```

### 6.4 需要额外建模的效果

标准仿真平台提供的相机模型通常缺少以下真实效果，需要自定义实现：

| 效果 | 真实相机表现 | 仿真实现方式 |
|------|------------|-------------|
| **ICR 彩转黑** | 夜间切换黑白+NIR | 后处理灰度化 + 场景 NIR 纹理 |
| **星光级低光噪声** | 光子散粒噪声主导 | 泊松-高斯噪声模型 |
| **62× 变焦** | 连续变焦，FOV 动态变化 | 运行时动态修改 FOV 参数 |
| **宽动态（WDR）** | 多次曝光合成 | HDR tone mapping 后处理 |
| **电子透雾** | 去雾算法 | UE5 雾效 + 暗通道先验去雾 |
| **电子防抖（EIS）** | 陀螺仪 + 裁剪补偿 | 抖动注入 + 稳像算法 |

**低光噪声的物理模型**：

真实 CMOS 在低光照下的噪声遵循泊松-高斯模型：

$$
I_{\text{noisy}} = \text{Poisson}(I_{\text{signal}} \cdot g) / g + \mathcal{N}(0, \sigma_{\text{read}}^2)
$$

其中 $g$ 是增益，$\sigma_{\text{read}}$ 是读出噪声。在仿真中可用以下 Python 代码后处理：

```python
import numpy as np

def add_starlight_noise(image, gain=10.0, read_noise_std=2.0, lux=0.002):
    signal = image.astype(np.float64) * (lux / 100.0)
    noisy = np.random.poisson(signal * gain) / gain
    noisy += np.random.normal(0, read_noise_std / 255.0, image.shape)
    return np.clip(noisy * (100.0 / max(lux, 0.001)), 0, 255).astype(np.uint8)
```

---

## 七、仿真建模：热成像仪

热成像仪的仿真比可见光复杂得多，因为它的成像原理完全不同——需要建模场景中每个物体的**温度**而非外观。

### 7.1 热成像仿真的本质

{% mermaid %}
graph LR
    A[场景物体] -->|可见光| B[反射光渲染<br>RGB 图像]
    A -->|热辐射| C[温度场渲染<br>灰度=温度]
    C --> D[NETD 噪声<br>+ NUC 残差]
    D --> E[伪彩色映射<br>+ AGC]
    E --> F[最终热像输出]
{% endmermaid %}

核心挑战：**每个物体需要一个"热纹理"而非"颜色纹理"**。

### 7.2 核心参数映射

| 物理参数 | 值 | 仿真参数 | 说明 |
|----------|---|----------|------|
| 分辨率 | 640×512 | `<width>` `<height>` | 直接映射 |
| 光谱范围 | 8-14 µm | 温度范围设定 | 决定可观测温度范围 |
| NETD | 30 mK | 噪声标准差 | $\sigma = 0.03$ K |
| 焦距范围 | 23-230 mm | `<horizontal_fov>` | 根据焦距计算 |
| 帧率 | 50 Hz | `<update_rate>` | 直接映射 |

**FOV 计算**（探测器尺寸按 17 µm 像素间距估算）：

$$
w_{\text{sensor}} = 640 \times 17\,\mu\text{m} = 10.88\,\text{mm}
$$

$$
\text{HFOV}_{23\text{mm}} = 2\arctan\left(\frac{10.88}{2 \times 23}\right) = 26.6°
$$

$$
\text{HFOV}_{230\text{mm}} = 2\arctan\left(\frac{10.88}{2 \times 230}\right) = 2.71°
$$

规格书标称 18.47° ～ 1.91°，差异表明探测器像素间距可能接近 12 µm 而非 17 µm（新一代非制冷探测器已实现 12 µm 像素间距）。

### 7.3 Gazebo Sim 热成像配置

Gazebo Sim（Citadel 及以上版本）原生支持热成像传感器：

**（1）传感器定义**

```xml
<sensor type="thermal_camera" name="ir_camera">
  <update_rate>50</update_rate>
  <camera name="thermal">
    <horizontal_fov>0.322</horizontal_fov>  <!-- 18.47° 广角端 -->
    <image>
      <width>640</width>
      <height>512</height>
      <format>L16</format>  <!-- 16-bit 灰度，分辨率 0.01K -->
    </image>
    <clip>
      <near>0.5</near>
      <far>5000</far>
    </clip>
  </camera>
  <always_on>1</always_on>
  <visualize>true</visualize>
  <topic>ir_camera</topic>
</sensor>
```

**（2）场景温度设定**

```xml
<!-- 世界环境温度 -->
<atmosphere>
  <temperature>288.15</temperature>          <!-- 15°C 地面温度 -->
  <temperature_gradient>-0.0065</temperature_gradient>  <!-- 标准递减率 -->
</atmosphere>

<!-- 无人机目标 - 高温 -->
<model name="target_drone">
  <link name="body">
    <!-- ... 几何/碰撞 ... -->
    <visual name="visual">
      <!-- ... 网格 ... -->
    </visual>
  </link>
  <plugin filename="gz-sim-thermal-system"
          name="gz::sim::systems::Thermal">
    <temperature>333.15</temperature>  <!-- 60°C，电机发热 -->
  </plugin>
</model>

<!-- 带热签名纹理的精细模型 -->
<model name="drone_detailed">
  <link name="body">
    <visual name="visual">
      <plugin filename="gz-sim-thermal-system"
              name="gz::sim::systems::Thermal">
        <temperature>293.15</temperature>       <!-- 基准 20°C -->
        <heat_signature>materials/drone_thermal.png</heat_signature>
        <min_temp>293.15</min_temp>              <!-- 热签名最低温 -->
        <max_temp>353.15</max_temp>              <!-- 热签名最高温 80°C -->
      </plugin>
    </visual>
  </link>
</model>
```

**（3）热签名纹理**

热签名（Heat Signature）是一张灰度纹理贴图，黑色对应 `min_temp`，白色对应 `max_temp`：

| 区域 | 灰度值 | 温度 | 对应部位 |
|------|--------|------|----------|
| 电机 | 255 (白) | 80°C | 四个角的电机位置 |
| 电池 | 200 | 65°C | 中心电池仓 |
| 机壳 | 100 | 38°C | 塑料/碳纤维外壳 |
| 桨叶 | 60 | 28°C | 接近环境温度 |

### 7.4 AirSim 红外成像配置

AirSim 的热成像仿真基于**分割图 ID 映射**方案：

```python
import airsim
import numpy as np

client = airsim.MultirotorClient()

IR_TEMP_MAP = {
    "target_drone": 180,
    "Sky":          20,
    "Ground":       120,
    "Tree":         100,
    "Building":     130,
}

for obj_name, digital_count in IR_TEMP_MAP.items():
    found = client.simSetSegmentationObjectID(
        obj_name, digital_count, is_name_regex=True
    )

responses = client.simGetImages([
    airsim.ImageRequest("front_center", airsim.ImageType.Infrared, False, False)
])
ir_image = np.frombuffer(responses[0].image_data_uint8, dtype=np.uint8)
ir_image = ir_image.reshape(responses[0].height, responses[0].width, 3)
```

AirSim 方案的核心是将物体的 Stencil ID 映射为"热数字计数"（0-255），红外图像读取时返回的就是这些数字计数值。但这种方式有明显局限：

| 局限 | 说明 |
|------|------|
| 仅 256 级温度 | 8-bit Stencil ID 限制 |
| 无温度梯度 | 同一物体温度均匀 |
| 无大气衰减 | 不模拟红外辐射在大气中的吸收散射 |
| 无 NETD 噪声 | 需要自行添加后处理噪声 |

### 7.5 Unreal Engine 自定义热成像后处理

对于更高保真度的热成像仿真，可在 UE5 中使用**后处理材质**（Post-Process Material）实现：

```
Custom Depth Stencil → 按 Stencil Value 分类 → 查表映射温度 → 灰度输出 → 伪彩色 LUT
```

核心步骤：

1. **启用 Custom Depth-Stencil Pass**（项目设置 → 渲染 → Custom Depth-Stencil Pass = Enabled with Stencil）
2. **为每个物体设置 Stencil Value**（0-255，映射到温度）
3. **创建后处理材质**，读取 Custom Stencil Buffer，按值映射到温度灰度
4. **添加 NETD 噪声**（30 mK 级高斯噪声）
5. **应用伪彩色 LUT**（铁红、彩虹、热白/热黑等）

### 7.6 热成像仿真的关键挑战与解决方案

| 挑战 | 说明 | 解决方案 |
|------|------|----------|
| **温度场建模** | 每个物体需要温度而非颜色 | 热签名纹理 + 温度数据库 |
| **大气透过率** | LWIR 在大气中有吸收（水汽、CO₂） | Beer-Lambert 衰减模型后处理 |
| **NETD 噪声** | 非制冷探测器固有噪声 | 高斯噪声 σ=30mK + 1/f 噪声 |
| **NUC 残差** | 非均匀性校正不完美，有固定图案噪声 | 固定图案噪声模板叠加 |
| **太阳加热效应** | 物体表面温度随日照变化 | 太阳辐射模型 + 热容/导热模型 |
| **天空背景温度** | 晴空天空有效辐射温度约 -40°C ～ -20°C | 大气辐射传输模型（MODTRAN 简化版） |
| **10× 变焦** | 焦距连续变化 | 运行时动态修改 FOV |

**大气衰减模型**（简化版 Beer-Lambert）：

$$
I_{\text{received}} = I_{\text{emitted}} \cdot \tau(R) + I_{\text{atm}}(R)
$$

$$
\tau(R) = e^{-\alpha \cdot R}
$$

其中 $\tau(R)$ 为距离 $R$ 处的大气透过率，$\alpha$ 为衰减系数（取决于温度、湿度），$I_{\text{atm}}$ 为大气路径辐射。

```python
import numpy as np

def atmospheric_attenuation_lwir(temp_object_K, distance_m, 
                                  humidity_pct=50, ambient_temp_K=288):
    """简化的 LWIR 大气衰减模型"""
    alpha = 0.0002 * (1 + humidity_pct / 100)  # 衰减系数
    tau = np.exp(-alpha * distance_m)
    
    temp_apparent = temp_object_K * tau + ambient_temp_K * (1 - tau)
    return temp_apparent

# 60°C 无人机在 2km 处的表观温度
apparent = atmospheric_attenuation_lwir(333.15, 2000)
print(f"2km 处表观温度: {apparent - 273.15:.1f}°C")
# 输出约 40-45°C（衰减后仍可探测）
```

---

## 八、双光融合仿真：完整配置示例

在 Gazebo Sim 中模拟 HP-Z50III 的双光 PTZ 配置，将可见光和热像相机安装在同一云台模型上：

### 8.1 PTZ 云台 SDF 模型

```xml
<model name="ptz_turret">
  <!-- 底座 -->
  <link name="base_link">
    <visual name="base_visual">
      <geometry><cylinder><radius>0.28</radius><length>0.3</length></cylinder></geometry>
      <material><ambient>0.3 0.3 0.3 1</ambient></material>
    </visual>
  </link>

  <!-- 水平旋转关节 -->
  <joint name="pan_joint" type="revolute">
    <parent>base_link</parent>
    <child>pan_link</child>
    <axis>
      <xyz>0 0 1</xyz>
      <limit><lower>-1e16</lower><upper>1e16</upper></limit>  <!-- 连续旋转 -->
      <dynamics><damping>0.5</damping></dynamics>
    </axis>
  </joint>

  <link name="pan_link">
    <visual name="pan_visual">
      <geometry><cylinder><radius>0.25</radius><length>0.15</length></cylinder></geometry>
    </visual>
  </link>

  <!-- 俯仰旋转关节 -->
  <joint name="tilt_joint" type="revolute">
    <parent>pan_link</parent>
    <child>sensor_head</child>
    <axis>
      <xyz>0 1 0</xyz>
      <limit>
        <lower>-1.5708</lower>   <!-- -90° -->
        <upper>1.5708</upper>    <!-- +90° -->
      </limit>
      <dynamics><damping>0.3</damping></dynamics>
    </axis>
  </joint>

  <!-- 传感器头部 - 搭载双光相机 -->
  <link name="sensor_head">
    <visual name="dome_visual">
      <geometry><sphere><radius>0.2</radius></sphere></geometry>
      <material><ambient>0.2 0.2 0.2 1</ambient></material>
    </visual>

    <!-- 可见光相机 -->
    <sensor type="camera" name="eo_camera">
      <pose>0.15 0 0 0 0 0</pose>
      <update_rate>50</update_rate>
      <camera name="visible">
        <horizontal_fov>0.583</horizontal_fov>
        <image>
          <width>2688</width>
          <height>1520</height>
          <format>R8G8B8</format>
        </image>
        <clip><near>1</near><far>10000</far></clip>
        <noise>
          <type>gaussian</type>
          <mean>0.0</mean>
          <stddev>0.003</stddev>
        </noise>
      </camera>
      <always_on>1</always_on>
      <topic>ptz/eo</topic>
    </sensor>

    <!-- 热成像相机 -->
    <sensor type="thermal_camera" name="ir_camera">
      <pose>0.15 0.05 0 0 0 0</pose>
      <update_rate>50</update_rate>
      <camera name="thermal">
        <horizontal_fov>0.322</horizontal_fov>
        <image>
          <width>640</width>
          <height>512</height>
          <format>L16</format>
        </image>
        <clip><near>1</near><far>5000</far></clip>
      </camera>
      <always_on>1</always_on>
      <topic>ptz/ir</topic>
    </sensor>
  </link>
</model>
```

### 8.2 云台伺服控制（gz-transport）

```python
#!/usr/bin/env python3
"""PTZ 云台伺服控制 - 模拟 HP-Z50III 的指向控制"""
from gz.transport13 import Node
from gz.msgs10.double_pb2 import Double
import math
import time

node = Node()

pan_pub = node.advertise("/model/ptz_turret/joint/pan_joint/0/cmd_pos", Double)
tilt_pub = node.advertise("/model/ptz_turret/joint/tilt_joint/0/cmd_pos", Double)

MAX_PAN_SPEED = math.radians(120)   # 120°/s
MAX_TILT_SPEED = math.radians(90)   # 90°/s
POSITIONING_ACCURACY = math.radians(0.005)  # 0.005°

def slew_to(pan_deg, tilt_deg, speed_factor=1.0):
    """指向目标方位"""
    msg = Double()
    msg.data = math.radians(pan_deg)
    pan_pub.publish(msg)

    msg.data = math.radians(max(-90, min(90, tilt_deg)))
    tilt_pub.publish(msg)

def track_target(target_az_deg, target_el_deg, fov_deg):
    """跟踪目标 - 焦距速度自适应"""
    speed_scale = fov_deg / 33.4
    slew_to(target_az_deg, target_el_deg, speed_factor=speed_scale)

if __name__ == "__main__":
    print("PTZ 云台控制启动")
    slew_to(0, 0)
    time.sleep(2)
    slew_to(180, 30)
    time.sleep(3)
    slew_to(270, -15)
```

---

## 九、仿真精度验证：真实参数 vs 仿真参数对照

### 9.1 可见光通道参数对照

| 参数 | 真实值 | Gazebo 仿真 | 保真度 | 备注 |
|------|--------|------------|--------|------|
| 分辨率 | 2688×1520 | 2688×1520 | **精确** | 直接映射 |
| FOV（广角） | 33.4° | 33.4° | **精确** | 直接设定 |
| FOV（长焦） | 0.58° | 运行时可调 | **精确** | 需动态修改 |
| 帧率 | 50 Hz | 50 Hz | **精确** | 直接设定 |
| 畸变 | 实测标定 | 径向+切向模型 | **近似** | 需用实际标定参数 |
| 低光噪声 | 泊松-高斯 | 高斯近似 | **粗略** | 需后处理增强 |
| ICR 彩转黑 | 自动切换 | 未建模 | **缺失** | 需自定义插件 |
| 宽动态 | 多帧合成 | 未建模 | **缺失** | 需 HDR 后处理 |
| 电子透雾 | 去雾算法 | 未建模 | **缺失** | 需自定义 |

### 9.2 热成像通道参数对照

| 参数 | 真实值 | Gazebo 仿真 | 保真度 | 备注 |
|------|--------|------------|--------|------|
| 分辨率 | 640×512 | 640×512 | **精确** | 直接映射 |
| 温度分辨率 | 10 mK (L16) | 10 mK | **精确** | L16 格式 |
| NETD | 30 mK | 需后处理添加 | **可实现** | σ=0.03K 高斯噪声 |
| 物体温度 | 连续分布 | 均匀/热签名 | **近似** | 热签名提升保真度 |
| 大气衰减 | 距离+湿度相关 | 未建模 | **缺失** | 需自定义后处理 |
| NUC 残差 | 固定图案噪声 | 未建模 | **缺失** | 需固定噪声模板 |
| 太阳加热 | 日照相关 | 未建模 | **缺失** | 需热力学模型 |
| 伪彩色 | 多种可选 | 需后处理 | **可实现** | LUT 映射 |

### 9.3 仿真保真度分级

{% mermaid %}
graph TB
    L1[Level 1: 基础热像<br>均匀温度 + 固定 FOV<br>Gazebo 原生支持] --> L2[Level 2: 增强热像<br>热签名纹理 + NETD 噪声<br>+ 动态 FOV]
    L2 --> L3[Level 3: 高保真热像<br>大气衰减 + NUC 残差<br>+ 太阳加热 + 日变化]
    L3 --> L4[Level 4: 物理级热像<br>MODTRAN 辐射传输<br>+ 材料发射率 + 风冷效应]
{% endmermaid %}

对于反无人机系统的**算法开发与验证**，Level 2 通常已经足够；对于**传感器性能评估**，需要 Level 3 及以上。

---

## 十、从仿真到实战：参数匹配清单

如果你手头有一台类似 HP-Z50III 的光电转台，以下是从真实设备到仿真模型的完整参数匹配清单：

### 10.1 必须匹配的参数（影响算法正确性）

| # | 参数 | 获取方式 | 仿真配置位置 |
|---|------|---------|-------------|
| 1 | 可见光分辨率 | 规格书 | `<image><width/height>` |
| 2 | 可见光 FOV 范围 | 规格书 / 标定 | `<horizontal_fov>` |
| 3 | 热像分辨率 | 规格书 | `<image><width/height>` |
| 4 | 热像 FOV 范围 | 规格书 / 标定 | `<horizontal_fov>` |
| 5 | 双光轴偏差 | 规格书（≤1/20 FOV） | 传感器 `<pose>` 微调 |
| 6 | PTZ 回转范围 | 规格书 | 关节 `<limit>` |
| 7 | PTZ 回转速度 | 规格书 | 关节速度控制器 |
| 8 | PTZ 定位精度 | 规格书 | 控制器死区 |

### 10.2 推荐匹配的参数（影响仿真保真度）

| # | 参数 | 获取方式 | 仿真配置位置 |
|---|------|---------|-------------|
| 9 | 镜头畸变系数 | 棋盘格标定 | `<distortion>` |
| 10 | NETD | 规格书 | 噪声后处理 σ |
| 11 | 目标热签名 | 红外实拍 / 测温 | 热签名纹理 |
| 12 | 背景温度分布 | 气象数据 + 测量 | 环境温度设定 |
| 13 | 帧率 | 规格书 | `<update_rate>` |

### 10.3 高级匹配（物理级仿真）

| # | 参数 | 获取方式 |
|---|------|---------|
| 14 | 大气透过率曲线 | MODTRAN 计算 / 实测 |
| 15 | 物体发射率 | 材料数据库 |
| 16 | 太阳辐射强度 | 气象站数据 |
| 17 | 风速（影响对流散热） | 气象站数据 |
| 18 | NUC 残差图案 | 实机采集死像元/残差图 |

---

## 十一、通信协议与系统集成

### 11.1 视频流接入

HP-Z50III 支持标准的网络视频协议，便于仿真系统集成：

| 协议 | 端口 | 用途 |
|------|------|------|
| RTSP | 554 | 实时视频流（主要） |
| HTTP | 80 | CGI 控制接口 |
| TCP | 39020 | 私有控制协议 |
| ONVIF 2.0 | — | 标准化设备发现与控制 |
| GB28181 | — | 国标视频监控联网 |

在仿真中，Gazebo 通过 `gz-transport` topic 发布图像，可通过 ROS2 bridge 转换为标准 `sensor_msgs/Image`，再通过 GStreamer 封装为 RTSP 流，模拟真实设备的视频输出：

{% mermaid %}
graph LR
    A[Gazebo 热像/可见光<br>gz-transport topic] --> B[ros_gz_bridge<br>Topic 转换]
    B --> C[ROS2 Image<br>sensor_msgs/Image]
    C --> D[GStreamer<br>RTSP 服务器]
    D --> E[反无人机指控系统<br>RTSP 客户端]
{% endmermaid %}

### 11.2 PTZ 控制接入

实际系统通过 TCP 39020 端口发送 JSON 命令控制云台：

```json
{
    "cmd": "ptzCtrl",
    "param": {
        "channelid": 0,
        "action": "moveTo",
        "pan": 180.0,
        "tilt": 30.0,
        "speed": 60.0
    }
}
```

在仿真中，可编写一个**协议适配器**，将真实的 TCP/JSON 控制命令转换为 Gazebo 关节控制指令，实现**真实指控软件直接驱动仿真 PTZ**：

```python
import socket
import json
from gz.transport13 import Node
from gz.msgs10.double_pb2 import Double
import math

class PTZProtocolAdapter:
    """将真实 PTZ 控制协议转换为 Gazebo 关节控制"""
    
    def __init__(self, listen_port=39020):
        self.node = Node()
        self.pan_pub = self.node.advertise(
            "/model/ptz_turret/joint/pan_joint/0/cmd_pos", Double
        )
        self.tilt_pub = self.node.advertise(
            "/model/ptz_turret/joint/tilt_joint/0/cmd_pos", Double
        )
        self.server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.server.bind(("0.0.0.0", listen_port))
        self.server.listen(1)
    
    def handle_command(self, data):
        try:
            msg = json.loads(data)
            if msg.get("cmd") == "ptzCtrl":
                param = msg["param"]
                pan_msg = Double()
                pan_msg.data = math.radians(param.get("pan", 0))
                self.pan_pub.publish(pan_msg)
                
                tilt_msg = Double()
                tilt_msg.data = math.radians(param.get("tilt", 0))
                self.tilt_pub.publish(tilt_msg)
        except (json.JSONDecodeError, KeyError):
            pass
    
    def run(self):
        while True:
            conn, addr = self.server.accept()
            data = conn.recv(4096).decode()
            self.handle_command(data)
            conn.close()
```

---

## 十二、总结与建议

### 12.1 两类相机的核心差异

| 维度 | 可见光相机 | 热成像仪 |
|------|-----------|---------|
| 感知对象 | 反射光（被动/主动） | 热辐射（纯被动） |
| 传感器 | CMOS 光电二极管 | VOx 微辐射热计 |
| 波段 | 0.4-1.0 µm | 8-14 µm |
| 分辨率 | 高（百万级） | 低（十万级） |
| 核心优势 | 高分辨率 + 彩色 | 全天候 + 温度信息 |
| 核心限制 | 光照依赖 | 分辨率低 |
| 仿真难度 | **低**（标准渲染管线） | **高**（需温度场建模） |

### 12.2 仿真建模建议

1. **先做可见光，再做热像**：可见光相机在所有仿真平台中都有良好支持，可以快速搭建起来验证算法框架
2. **热像仿真优先用 Gazebo**：Gazebo Sim 原生支持热成像传感器，包括温度设定、热签名纹理、16-bit 输出，是目前开源方案中最完整的
3. **UE5 适合高保真可见光**：如果需要逼真的日夜变化、天气效果、复杂场景渲染，UE5 + AirSim/Colosseum 是更好的选择
4. **NETD 噪声必须添加**：30 mK 的 NETD 噪声对算法鲁棒性有显著影响，不加噪声训练的检测模型在真实热像上会严重退化
5. **热签名纹理是关键**：均匀温度的物体模型过于理想化，制作热签名纹理（电机热、电池热、外壳温）大幅提升仿真保真度
6. **协议适配器实现 SIL**：通过模拟真实的控制协议接口，可以让真实的指控软件直接驱动仿真环境，实现软件在环测试（SIL）

---

## 参考资料

1. HP-Z50IIIBL-TC61023-VC4075-AU-SFW7 光电转台技术规格书，济南和普威视光电技术有限公司，2025
2. Gazebo Sim Thermal Camera Tutorial, [gazebosim.org](https://gazebosim.org/api/sensors/9/thermalcameraigngazebo.html)
3. AirSim Infrared Camera, [microsoft/AirSim](https://github.com/microsoft/AirSim/blob/master/docs/InfraredCamera.md)
4. Bondi et al., "AirSim-W: A Simulation Environment for Wildlife Conservation with UAVs", ACM SIGCAS 2018
5. Johnson, J., "Analysis of image forming systems", Image Intensifier Symposium, 1958
6. Rogalski, A., "Infrared Detectors", CRC Press, 2010
7. Holst, G.C., "Electro-Optical Imaging System Performance", SPIE Press, 2008
